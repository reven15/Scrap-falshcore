services:
  flashscore-odds:
    build: .
    image: flashscore-odds-scraper:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    # Cada scrape usa memória a sério (Chromium + páginas). Ajusta ao
    # que o teu home server aguentar.
    mem_limit: 1g
    shm_size: "1gb"
